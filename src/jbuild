(library (
  (name hardcaml_hobby_boards)
  (libraries (base hardcaml parsexp rope stdio simple_xml))
  (preprocess (pps (ppx_hardcaml ppx_jane ppx_rope)))))

(embed_file (
  (target      board_info)
  (with_alist  true)
  (apply_style true)
  (sources (nexys-a7-100t.sexp arty-a7-35.sexp arty-a7-100.sexp))))
